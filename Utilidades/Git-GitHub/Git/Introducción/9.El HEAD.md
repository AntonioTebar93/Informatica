
El puntero de referencia, el punto actual del historial de cambios del repositorio.

El valor de HEAD puede averiguarse con el comando:

`$ git symbolic-ref HEAD`

o con este, con el que obtenemos la numeración del HEAD

`$ git rev-parse HEAD`





